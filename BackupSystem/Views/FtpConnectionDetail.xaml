<Page x:Class="BackupSystem.FtpConnectionDetail"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:BackupSystem.ViewModel"
        Height="300" Width="300">
    <Page.DataContext>
        <vm:FtpConnectionDetailViewModel />
    </Page.DataContext>
    <Grid>
        <DataGrid ItemsSource="{Binding FtpConnectionList}">
            
        </DataGrid>
    </Grid>
</Page>
